diff --git a/lib/commands/new.js b/lib/commands/new.js
index 515dfce..b3275db 100644
--- a/lib/commands/new.js
+++ b/lib/commands/new.js
@@ -13,6 +13,17 @@ const NAO_SAO_TEMAS = new Set(['base', 'responsive']);
 // como recomendado. Lista alfabética esconderia a recomendação no meio.
 export const DECK_PADRAO = 'mira-default';
 
+// Templates que trazem o próprio tema e NÃO aceitam --theme. O bloco
+// @MIRA:THEME deles existe para o contrato de esqueleto do /mira-fast, não
+// para ser substituído: injetar tema + base.css neles atropelaria o layout
+// (o base.css impõe altura fixa e fundo ao palco e ao body).
+export const THEME_AGNOSTIC = new Set([
+  'sandeco-just-animation-template',
+  'mira-studio-demo',
+  'mira-studio-full-demo',
+  'mesa-tatica',
+]);
+
 function listarDecks() {
   const dir = join(MIRA_ROOT, 'templates', 'decks');
   if (!existsSync(dir)) return [];
@@ -96,20 +107,29 @@ export default async function newDeck(args) {
   let html = readFileSync(deckSrc, 'utf8');
 
   // 2. Injeta o tema escolhido (inline, para o deck ser auto-contido).
-  //    Exceção: o animation-pure é theme-agnóstico e mantém o próprio bloco
-  //    @MIRA:THEME, ignorando --theme, porque é multicor: a cor vive na paleta
-  //    do <script>, não numa primária única.
+  //    Exceção: os templates de THEME_AGNOSTIC mantêm o próprio bloco
+  //    @MIRA:THEME e ignoram --theme. O animation-pure porque é multicor (a cor
+  //    vive na paleta do <script>); os Studio e a mesa tática porque o layout
+  //    deles não sobrevive ao base.css que viria junto do tema.
   //    O mira-default NÃO entra aqui: ele aceita tema de propósito (é o padrão
   //    do Mira, trocar de tema tem que funcionar). Ele neutraliza no próprio
   //    CSS o que o base.css impõe ao palco (altura fixa, glow, cantos).
-  const animationOnly = deck === 'sandeco-just-animation-template';
-  if (!animationOnly) {
+  const themeAgnostic = THEME_AGNOSTIC.has(deck);
+  if (!themeAgnostic) {
     const themeCss = readFileSync(join(MIRA_ROOT, 'templates', 'themes', `${theme}.css`), 'utf8');
     const baseCss = readFileSync(join(MIRA_ROOT, 'templates', 'themes', 'base.css'), 'utf8');
+    const antes = html;
     html = html.replace(
       /\/\* @MIRA:THEME:START \*\/[\s\S]*?\/\* @MIRA:THEME:END \*\//,
       '/* @MIRA:THEME:START */\n' + themeCss + '\n\n' + baseCss + '\n/* @MIRA:THEME:END */'
     );
+    // Sem o par de marcadores o replace não casa e devolve a string intacta.
+    // Falhar em silêncio aqui já custou um bug: avise em vez de prometer.
+    if (html === antes) {
+      console.warn(`\n  Aviso: o template "${deck}" não tem o bloco @MIRA:THEME.`);
+      console.warn(`  O tema "${theme}" NÃO foi aplicado. Acrescente os marcadores ao template,`);
+      console.warn(`  ou declare-o em THEME_AGNOSTIC se ele traz o próprio tema de propósito.`);
+    }
   }
 
   // 2.1. Camada de responsividade (reflow mobile-first). Vale para TODO deck,
@@ -156,7 +176,7 @@ export default async function newDeck(args) {
   saveConfig(config);
 
   console.log(`\n  Deck "${name}" criado em decks/${name}/index.html`);
-  console.log(`  Template: ${deck} | Tema: ${animationOnly ? 'próprio do template (theme-agnóstico)' : theme}`);
+  console.log(`  Template: ${deck} | Tema: ${themeAgnostic ? 'próprio do template (theme-agnóstico)' : theme}`);
   if (offline) {
     console.log(`  Offline: libs locais em assets/vendor/ (${offline.copied} arquivos) — abre por file:// sem internet.`);
   } else {
