diff --git a/templates/decks/mira-studio-demo/index.html b/templates/decks/mira-studio-demo/index.html
index a5db515..837f96a 100644
--- a/templates/decks/mira-studio-demo/index.html
+++ b/templates/decks/mira-studio-demo/index.html
@@ -337,11 +337,17 @@
            Estrutura (layout e título) é lida UMA vez, no load: mudou, recarregue.
            Só o TEXTO sincroniza ao vivo (ver o bloco do TELEPROMPTER).
 
-           Este deck tem animação AUTORAL: o builder cria o palco vazio
-           (svg#sv-slide-N, N = posição no roteiro) e cada animação escrita à
-           mão se prende ao seu palco. Palco sem animação fica vazio, não quebra.
-           Chrome autoral dentro da <section> (logo, selo .me-ov) precisa ser
-           emitido AQUI, senão a reconstrução o descarta.
+           PRESERVAR É O PADRÃO. A seção que já está no HTML manda na ESTRUTURA
+           (palco, ids, chrome autoral); o roteiro.md manda no TÍTULO e no TEXTO.
+           O builder só recria uma seção do zero quando não existe seção naquela
+           posição, ou quando o roteiro pede outro layout. É isso que mantém de
+           pé tanto o deck gerado pelo /mira-fast (palcos <slug>-stage /
+           <slug>-svg, com animação própria) quanto o deck escrito à mão.
+
+           Este deck tem animação AUTORAL presa a svg#sv-slide-N (N = posição no
+           roteiro). Quando uma seção não traz palco, ou traz um <svg> sem id, o
+           builder assume e usa esse id genérico. Palco sem animação fica vazio,
+           não quebra.
            ============================================================ */
         (function () {
             /* fala de cada slide quando não há roteiro.md (file:// ou sem servidor) */
@@ -446,9 +452,47 @@
                 return '<div class="anim-stage"><svg id="sv-slide-' + n +
                     '" preserveAspectRatio="xMidYMid meet"></svg></div>';
             }
-            function montarSecao(s, n) {
+
+            /* layout que uma seção JÁ montada representa (mesma regra do semear) */
+            function layoutDe(sec) {
+                return sec.getAttribute('data-layout') ||
+                    (sec.classList.contains('capa') ? 'capa' : 'camera');
+            }
+
+            /* O palco é do deck, não do builder. Se a seção já traz um, ele fica
+               inteiro, com os ids que tiver: é assim que o palco gerado pelo
+               /mira-fast (<slug>-stage / <slug>-svg) sobrevive à reconstrução.
+               Só quando não há palco nenhum, ou quando o <svg> está sem id, o
+               builder assume e usa o sv-slide-N que a animação autoral espera. */
+            function garantirPalco(sec, n) {
+                var stage = sec.querySelector('.anim-stage');
+                if (!stage) {
+                    (sec.querySelector('.split-top, .full-wrap') || sec)
+                        .insertAdjacentHTML('beforeend', palco(n));
+                    return;
+                }
+                var svg = stage.querySelector('svg');
+                if (svg && !svg.id) svg.id = 'sv-slide-' + n;
+            }
+
+            function montarSecao(s, n, original) {
                 var sec;
-                if (s.layout === 'capa' && capaBase) {
+                var alvo = s.layout || 'camera';
+
+                /* BUG-20260731-JZNJ · REAPROVEITAMENTO POR POSIÇÃO (R-09).
+                   A seção que já está no DOM
+                   manda na ESTRUTURA (palco, ids, chrome autoral); o roteiro.md
+                   manda no TEXTO e no título. Recriar do zero é a exceção: só
+                   quando não existe seção nesta posição, ou quando o roteiro pede
+                   outro layout. Antes a reconstrução era incondicional e jogava
+                   fora o palco do deck, deixando toda animação muda sob HTTP. */
+                if (original && layoutDe(original) === alvo) {
+                    if (s.titulo) tituloEm(original.querySelector('h1, h2'), s.titulo);
+                    if (alvo === 'split' || alvo === 'full') garantirPalco(original, n);
+                    return original;
+                }
+
+                if (alvo === 'capa' && capaBase) {
                     sec = capaBase.cloneNode(true);
                     if (s.titulo) tituloEm(sec.querySelector('h1'), s.titulo);
                     return sec;
@@ -512,9 +556,14 @@
             R.raw = raw;
             window.__miraRoteiro = R;
 
-            var novas = R.slides.map(function (s, i) { return montarSecao(s, i + 1); });
+            var novas = R.slides.map(function (s, i) { return montarSecao(s, i + 1, estaticas[i]); });
             var ancora = document.getElementById('mira-prompter');
-            estaticas.forEach(function (sec) { sec.parentNode.removeChild(sec); });
+            /* só sai do DOM a seção que NÃO foi reaproveitada */
+            estaticas.forEach(function (sec) {
+                if (novas.indexOf(sec) === -1) sec.parentNode.removeChild(sec);
+            });
+            /* insertBefore também REPOSICIONA quem já está no documento: as
+               reaproveitadas acabam na ordem do roteiro sem perder identidade */
             novas.forEach(function (sec) { document.body.insertBefore(sec, ancora); });
         })();
 
