<!-- GENERATED, DO NOT EDIT: regenerado por /reversa-debugger-graph em 2026-08-01T21:56:00Z a partir de 1 bugs -->

# Matriz de relações BUG ↔ BUG · modo-edicao

## Arestas canônicas gravadas neste contexto

Nenhuma. O contexto tem um único bug e nenhuma relação proposta com bugs de outros contextos.

## Arestas que chegam de outro contexto

Nenhuma.

## Vínculo não modelado como aresta

O `BUG-20260801-6UHJ` tem parentesco com uma correção **anterior ao registro de bugs**, feita fora
do `_reversa_bugs/`: `_reversa_sdd/edit-stuck-fix/`. Aquele ciclo tratou o mesmo relato do usuário
("edito, salvo e trava; só o F5 resolve") por outra causa raiz: `textCtx` e
`body.mef-text-editing` presos quando o `blur` não chegava. Como não existe `bug.md` para aquele
caso, não há aresta `regression-of` para gravar; a ligação está registrada em prosa nas Agent Notes
do 6UHJ e em `spec-matrix.md`.
